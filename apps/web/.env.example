# PAAX Web App — Environment Variables
# Copy ke .env.local dan sesuaikan bila perlu.

# URL Core Engine (FastAPI). Halaman /rab-tester memanggil engine ini.
NEXT_PUBLIC_CORE_ENGINE_URL=http://localhost:8081
